import SwiftUI

struct Page1View: View {
    var body: some View {
        Text("Hello from page 1")
            .fontWeight(.medium)
            .textScale(.secondary)
            .foregroundStyle(.gray)
    }
}
