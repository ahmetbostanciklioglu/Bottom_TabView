import SwiftUI

struct Page3View: View {
    var body: some View {
        Text("Hello from page 3")
            .fontWeight(.medium)
            .textScale(.secondary)
            .foregroundStyle(.gray)
    }
}
